# 更新记录

本项目遵循语义化版本号。每个版本在通过离线回归、便携自检和私有数据扫描后再提交并同步到 GitHub。

## 0.1.15 - 2026-08-30

- API JSON、媒体下载和远程缩略图现在共用严格有界的 `Content-Length` 解析器；只有响应头真正缺失时才允许未知长度，显式空值、符号、空白、非 ASCII 数字、超长整数以及相同或冲突的重复字段都会失败关闭。原生 WinHTTP 原始响应头不再静默保留最后一个重复长度。
- 任何已声明的响应长度都必须与本次实际接收的正文完全一致；超出声明时立即停止，不足声明时在 EOF 拒绝。可重放 API GET 仍按既有预算重试，密码或变更 POST 仍不重放；缩略图不发出部分成功，206 续传则独立核对本次响应字节与完整资源位置。

## 0.1.14 - 2026-08-30

- `sample`、`preview` 与远程缩略图现在除了文件签名，还要求 JPEG 物理结尾 EOI、PNG 最终 IEND、GIF Trailer 或与实际长度一致的 WebP RIFF 边界；只有三字节 JPEG 文件头、截断内容和使文件不再以相应终止边界结束的尾随字节不再能保存或标记为缩略图成功。该门槛明确只验证必要容器边界，不冒充像素解码或完整中部结构验证。
- 普通文件与绑定根目录的本地复核会在同一次有界散列读取中保留固定长度文件尾，不按路径重新打开，也不聚合大型媒体；已保存的衍生文件因此应用同一边界规则。原图继续遵循站点长度与 MD5 契约，严格的本地缩略图元数据策略也保持独立。

## 0.1.13 - 2026-08-30

- 下载项的成功或失败终态会在写盘前保留为当前批次的精确意图；首次持久化失败时由批次收尾原样重试，不再把已完成文件或原始错误泛化覆盖成待处理。若终态持续无法确认保存，该项会排除在批次兜底之外并明确提示重启后核对，界面不再宣称状态已经恢复或持久化。
- 媒体响应中的超长纯数字和非 ASCII 数字 `Content-Length`、超界 `Content-Range` 现在会稳定归类为下载协议错误；解析在进入 Python 大整数转换前有界拒绝，续传文件与状态保持不变，不会再泄漏为内部 `ValueError`。
- 便携组装与确定性 ZIP 会在路径规范化前检查调用方给出的源码和 Runtime 根目录；目录符号链接及 Windows 重解析点不能再因 `resolve()` 抹去链接身份而绕过普通目录发布边界。

## 0.1.12 - 2026-08-30

- API、媒体下载与缩略图共用唯一的限流响应解析器；`Retry-After` 中的 `NaN`、正负无穷、负数和溢出值不再变成零秒或 24 小时冷却，也不会遮蔽有效的 `X-RateLimit-Reset`。共享请求门与 API 全局门同时拒绝非有限延期，所有回退结果保持在有限安全范围内。
- 任务篮现在从同一份有界二进制快照解析并绑定 SHA-256 内容摘要，跨进程锁覆盖重检、原子替换与写后确认；即使外部更新保持文件大小和时间戳不变，也不能被旧实例静默覆盖。编码结果超过 16 MiB 会在任何磁盘替换前原子拒绝，载入替换竞态与失败路径继续保留原磁盘和内存状态。

## 0.1.11 - 2026-08-30

- 设置文件遇到权限、共享冲突等暂时读取故障时会原样保留，不再误判为损坏、隔离文件或清除仍有效的 DPAPI 凭据；本次启动会禁用设置保存、凭据恢复与变更，以及依赖推测下载目录的扫描和下载，重启读取成功后自动恢复正常状态。合法 JSON 中的极端溢出数值也会按确认损坏安全隔离，不会逃逸并中断启动。
- 本地下载库在搜索、筛选、排序和同序报告刷新后会按相对路径保留仍可见的多选、当前焦点列、视口锚点与离线预览；同一行内容变化只重绘该行并重建预览，切换下载根目录时会按根身份重新绑定，隐藏项不会在清空筛选后意外复活。

## 0.1.10 - 2026-08-30

- 可重放的 GET 若在读取 JSON 正文时遇到传输故障，现在会按既定预算重试；每次失败响应以及 429、408、5xx 响应都会先释放原生请求与连接句柄，再执行退避或 `Retry-After` 等待，重试耗尽后的界面错误保持脱敏。
- 取消操作在正文静默结束、传输异常和 JSON 解析竞态中都优先报告为“已取消”，不会误报损坏 JSON 或发起下一次请求；所有 POST 在方法层强制零重放，即使未来调用方遗漏参数也不会重复发送密码或变更请求。

## 0.1.9 - 2026-08-30

- 当前下载批次拥有的任务会一直保护到线程 `finished` 完全交付；任务页会禁用移除操作并在混合选择中整批拒绝，不会再出现任务记录已删除、媒体仍继续下载且终态无法写回的情况。批次外的非活动任务仍可单独移除。
- `TaskStore` 同步增加活动态不变量：任何删除集合只要包含排队或运行任务便在写盘前原子拒绝；无效、缺失与重复 ID 继续遵循既有合同，批次结束恢复为可移除状态后只提交一次 revision。

## 0.1.8 - 2026-08-30

- 任务表在状态写盘、筛选或其他模型刷新后按作品 ID 恢复仍可见的多选、当前焦点列和顶部视口锚点；行号变化不会把操作对象切到另一项，隐藏或删除的任务也不会在后续刷新中重新被选中。同序状态更新只重绘变化行，大规模选择捕获改用选择区间，避免 10,000 项任务上反复重置整表造成卡顿。

## 0.1.7 - 2026-08-30

- 下载批次若在线程创建、启动、API 或下载器初始化阶段失败，会报告脱敏原因并原子恢复该批次仍处于排队/运行的任务；每条异步下载信号都绑定所属线程，迟到信号不能改写新批次。站点阻断任务回到待处理，用户主动停止仍标记为已取消，恢复写盘失败也会明确提示。
- 确定性 ZIP 工具自身现在强制 `Data/` 与 `Downloads/` 使用规范名称、必须存在且为普通空目录；只写入两个空目录项，从不遍历私有内容，并在归档发布前重复检查，手工误用也不会把设置、DPAPI 文件或媒体写入 ZIP。

## 0.1.6 - 2026-08-30

- 修复 Windows runner 将同一临时目录分别表示为 8.3 短路径和长路径时的发行自检单测误报；测试现在按生产代码的规范化路径合同断言，不放宽任何 Runtime、codec 或便携发布门禁。

## 0.1.5 - 2026-08-30

- 下载任务页新增作品 ID、输出文件和错误说明的有界多条件搜索，以及六种任务状态筛选；10,000 项任务改用惰性表格模型，筛选只影响视图，“下载全部待处理”仍作用于完整任务篮。
- 断点续传在媒体签名与摘要校验前复核磁盘实际大小；若另一写者并发追加同一 `.part`，本次下载失败关闭并保留未完成文件与状态，不会把污染内容提交为完成媒体。
- 设置只接受便携根下规范化的 `Downloads` 相对路径或明确的 Windows 绝对路径；任务输出路径拒绝盘符相对路径、ADS、设备名、尾随点/空格和越界段。
- Runtime 构建失败现在输出失败步骤、退出码及有界 stdout/stderr；离线窗口烟测会直接显示页签合同的 expected/actual。验证临时根移出被审计 Runtime，避免并行合规检查看见瞬时额外载荷，同时保持确定性 report 与发行物不含诊断文本。

## 0.1.4 - 2026-08-29

- 本地下载库将权限拒绝、Windows 共享/文件锁冲突和已打开文件的普通 I/O 故障明确归为“无法读取”，不再与路径重定向风险混为“路径不安全”。
- POSIX errno 与 Windows NTSTATUS 只采用保守白名单；链接、重解析点、目录、非普通文件和未知错误仍失败关闭为“路径不安全”，不会放宽任何读取或完整性校验。
- 元数据首次读取、媒体读取以及媒体校验后的元数据复读均保留准确状态；所有底层路径和原生错误继续脱敏，复读失败仍报告已完成媒体校验的字节数。
- 修复发行构建的离线窗口烟测与五页签共享合同不同步，避免当前页签集合被陈旧的硬编码数量误判为失败。

## 0.1.3 - 2026-08-29

- 本地下载库新增有界多条件搜索、七种完整性状态筛选，以及按作品 ID、创作时间和文件大小排序。
- 新增已验证 JPEG、PNG、WebP 的只读离线预览；本地库一次扫描全程固定同一根目录句柄，名称、sidecar 与媒体都相对该句柄读取，预览再绑定报告中的大小、SHA-256 与内容类型，目录或文件被替换时失败关闭。
- 图片进入 Qt 解码器前先经过严格 PNG/JPEG/WebP 结构白名单；拒绝文本、ICC/EXIF/XMP、动画与未知块，并限制输入为 20 MiB、解码像素为 1600 万。
- 预览缩略图从纯像素副本生成，选择快速切换时只保留最新请求，关闭窗口会取消并等待活动预览线程。
- 修复下载线程终态与新任务之间的所有权竞争；发布自检改用共享五页签合同，并新增 PNG/JPEG/WebP 实际编解码门禁。

## 0.1.2 - 2026-08-29

- 新增“本地下载库”：仅扫描下载目录第一层，离线复核 sidecar、SHA-256、媒体签名与文件身份，按七种完整性状态筛选。
- 扫描是只读、可取消且事务化提交报告；取消或失败保留上次完整结果，10,000 项上限通过惰性表格模型显示。
- 持久凭据改为无秘密事务 journal 与 vault receipt 绑定；崩溃后只能恢复新会话或禁用自动加载，不会回退激活旧会话。
- 启用、禁用和启动恢复共用跨进程锁；不含 receipt 的旧记住状态安全失效，升级后需重新登录一次。
- 加固本地元数据：媒体长时间哈希后再次有界、no-follow 读取 sidecar；超深 JSON、路径替换和大小写 `.JSON` 孤立旁车均有回归覆盖。

## 0.1.1 - 2026-08-29

- 搜索支持取消，分页结果与游标改为事务化提交，取消或失败不再留下半页状态。
- 单项作品遇到 403 时只失败该项并继续同批任务，不再阻断整批。
- TaskStore 恢复态回写失败时不再把可读任务文件误隔离为损坏文件。
- 持久凭据升级为 schema 2，不再保存密码；设置与加密会话写入失败时一致回滚。

## 0.1.0 - 2026-08-29

- 首个可用版本：原生标签搜索、Safe 默认分级、缩略图网格和受限站内浏览。
- 支持账号令牌登录、Windows DPAPI 可选保存；密码与令牌不进入普通设置、日志或任务文件。
- 提供持久化任务篮、单实例保护、顺序下载、取消、重试和异常退出恢复。
- 下载前刷新作品元数据；官方媒体域逐跳校验；严格 Range/If-Range 续传、大小/签名/MD5 校验和原子 no-replace 提交。
- original/sample/preview 分别命名和校验；派生媒体按实际 MIME 与文件签名决定扩展名，歧义容器失败关闭；同名结果安全编号，不覆盖既有文件。
- 提供相对默认下载目录、可选脱敏 JSON 元数据、代理设置和保守限流/退避。
- 加入离线测试、Windows CI、便携发行门禁、SHA-256 清单生成和第三方组件说明。
- 应用 HTTP 传输改为 Windows WinHTTP/Schannel，移除 requests/urllib3/PySocks、Python OpenSSL 扩展、OpenSSL DLL 与 Qt OpenSSL TLS 插件；强制 TLS 1.2、证书吊销检查、手动跳转和无 Cookie/环境凭据会话。
- 正式启动在导入 Qt WebEngine 前清理继承的调试、禁沙箱、代理和 TLS key-log 环境变量，并强制 Qt Schannel 后端。
- Runtime、许可证、SBOM 与 OpenVEX 采用可复现冻结输出；确定性 ZIP 的重复构建必须逐字节一致。
- 便携树相对路径统一限制为 160 个字符；超长 Qt 归属页使用可读前缀加完整 SHA-256 的稳定文件名，普通 Windows Git 配置也能直接检出。
- 修复 isolated embedded Python 直接执行发布自检与合规工具时的同目录导入，发布工作流和一键验包使用同一入口。
